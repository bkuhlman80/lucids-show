---
import '../styles/global.css';
import Logo from '../components/Logo.astro';

const siteUrl = 'https://lucids.show';
const pageTitle = 'LUCIDS - Dream-tenders of the Hallow';
const pageDescription = 'An audio drama podcast for children ages 8-12, set in the bioluminescent underground world of the Hallow.';
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />

		<!-- Google Fonts -->
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;500;600;700&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />

		<!-- Primary Meta Tags -->
		<title>{pageTitle}</title>
		<meta name="title" content={pageTitle} />
		<meta name="description" content={pageDescription} />

		<!-- Open Graph / Facebook -->
		<meta property="og:type" content="website" />
		<meta property="og:url" content={siteUrl} />
		<meta property="og:title" content={pageTitle} />
		<meta property="og:description" content={pageDescription} />
		<meta property="og:image" content={`${siteUrl}/og-image.png`} />

		<!-- Twitter -->
		<meta property="twitter:card" content="summary_large_image" />
		<meta property="twitter:url" content={siteUrl} />
		<meta property="twitter:title" content={pageTitle} />
		<meta property="twitter:description" content={pageDescription} />
		<meta property="twitter:image" content={`${siteUrl}/og-image.png`} />
	</head>
	<body>
		<!-- Header -->
		<header class="site-header">
			<div class="container">
				<nav class="nav">
					<a href="/" class="logo-link">
						<Logo />
					</a>
					<a href="#about" class="nav-link">About</a>
				</nav>
			</div>
		</header>

		<!-- Hero Section -->
		<section class="hero">
			<div class="container">
				<div class="hero-content">
					<h1 class="hero-title">
						<span class="text-glow">Where Dreams Come Alive</span>
					</h1>
					<p class="hero-subtitle text-lg">
						LUCIDS is a family-friendly audio drama about magical creatures who tend children's dreams—exploring the full range of emotions with care, honesty, and original music in every episode.
					</p>
					<div class="hero-cta">
						<div class="signup-card">
							<form name="email-signup" method="POST" data-netlify="true" netlify-honeypot="bot-field" class="signup-form">
								<input type="hidden" name="form-name" value="email-signup" />
								<p class="hidden">
									<label>Don't fill this out if you're human: <input name="bot-field" /></label>
								</p>
								<div class="form-group">
									<input
										type="email"
										name="email"
										placeholder="your@email.com"
										required
										class="email-input"
										aria-label="Email address"
									/>
									<button type="submit" class="btn btn-cta">Notify Me at Launch</button>
								</div>
							</form>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- About Section -->
		<section id="about" class="section-about">
			<div class="container">
				<h2 class="text-amber">About LUCIDS</h2>
				<div class="about-content">
					<p class="text-lg">
						Deep beneath the waking world lies the Hallow—a bioluminescent realm where creatures called Lucids tend the dreams of sleeping children. Each episode follows these dream-tenders as they craft nighttime adventures, navigate unexpected challenges, and discover that the dreams they're making often reflect something they need to learn themselves.
					</p>
					<p>
						LUCIDS is designed for listeners ages 8-12, but crafted to reward the whole family. Littles will love the music and the characters. Bigger kids will catch the emotional depth. Parents will appreciate a show that treats children's inner lives with respect.
					</p>
					<div class="about-features">
						<div class="feature-card card">
							<h3>Original Music Every Episode</h3>
							<p class="text-secondary">Each episode features original songs that move the story forward—not filler, but genuine musical storytelling across genres from Latin urbano to K-pop to indie folk.</p>
						</div>
						<div class="feature-card card">
							<h3>Emotional Honesty</h3>
							<p class="text-secondary">We don't shy away from big feelings. Anxiety, grief, joy, resilience—LUCIDS explores the full emotional landscape of childhood with care and without condescension.</p>
						</div>
						<div class="feature-card card">
							<h3>Audio-First Adventure</h3>
							<p class="text-secondary">Built for listening. Whether it's bedtime, a road trip, or screen-free time, LUCIDS creates vivid worlds through sound design, voice performance, and music.</p>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Contact Section -->
		<section id="contact" class="section-contact">
			<div class="container">
				<h2 class="text-amber">Send Us a Dream</h2>
				<div class="contact-content">
					<p class="text-lg">
						Text us, call us, or send an email—we'd love to hear from your family.
					</p>
					<div class="contact-methods">
						<p class="contact-method">
							📱 <a href="tel:+18012038149" class="text-brand">(801) 203-8149</a>
						</p>
						<p class="contact-method">
							✉️ <a href="mailto:friends@lucids.show" class="text-brand">friends@lucids.show</a>
						</p>
					</div>
				</div>
			</div>
		</section>

		<!-- Footer -->
		<footer class="site-footer">
			<div class="container">
				<div class="footer-content">
					<div class="footer-brand">
						<Logo class="footer-logo" />
						<p class="text-sm text-secondary">Dream-tenders of the Hallow</p>
					</div>
					<div class="footer-links">
						<p class="text-sm">
							<a href="mailto:friends@lucids.show" class="text-brand">friends@lucids.show</a>
						</p>
						<p class="text-sm text-secondary">[Social media links]</p>
					</div>
				</div>
				<div class="footer-legal">
					<p class="text-xs text-secondary">
						© {new Date().getFullYear()} LUCIDS. All rights reserved.
					</p>
				</div>
			</div>
		</footer>
	</body>
</html>

<style>
	/* Header */
	.site-header {
		padding: var(--space-6) 0;
		border-bottom: 1px solid var(--color-border);
	}

	.nav {
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.logo-link {
		display: block;
		width: 180px;
	}

	.nav-link {
		font-family: var(--font-display);
		font-weight: var(--weight-medium);
		color: var(--color-text-primary);
		transition: color 0.2s ease;
	}

	.nav-link:hover {
		color: var(--color-brand);
	}

	/* Hero */
	.hero {
		padding: var(--space-20) 0;
		text-align: center;
	}

	.hero-content {
		max-width: 800px;
		margin: 0 auto;
	}

	.hero-title {
		font-size: var(--text-5xl);
		margin-bottom: var(--space-6);
		line-height: var(--leading-tight);
	}

	.hero-subtitle {
		margin-bottom: var(--space-8);
		color: var(--color-text-secondary);
	}

	.hero-cta {
		display: flex;
		gap: var(--space-4);
		justify-content: center;
		flex-wrap: wrap;
	}

	/* About Section */
	.section-about {
		padding: var(--space-20) 0;
	}

	.about-content {
		max-width: 100%;
	}

	.about-content > p {
		max-width: 75ch;
	}

	.about-features {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: var(--space-6);
		margin-top: var(--space-12);
	}

	.feature-card h3 {
		font-size: var(--text-xl);
		margin-bottom: var(--space-3);
	}

	/* Contact Section */
	.section-contact {
		padding: var(--space-20) 0;
		background-color: var(--color-cavern-mid);
	}

	.contact-content {
		max-width: 700px;
		margin: 0 auto;
		text-align: center;
	}

	.contact-widget {
		margin-top: var(--space-8);
		padding: var(--space-12);
	}

	/* Footer */
	.site-footer {
		padding: var(--space-12) 0 var(--space-6);
		border-top: 1px solid var(--color-border);
	}

	.footer-content {
		display: flex;
		justify-content: space-between;
		align-items: flex-start;
		margin-bottom: var(--space-8);
		flex-wrap: wrap;
		gap: var(--space-8);
	}

	.footer-brand {
		max-width: 200px;
	}

	.footer-logo {
		margin-bottom: var(--space-2);
	}

	.footer-legal {
		text-align: center;
		padding-top: var(--space-6);
		border-top: 1px solid var(--color-border);
	}

	/* Email Signup Card */
	@keyframes glow-pulse {
		0%, 100% {
			box-shadow: 0 0 20px rgba(127, 219, 202, 0.3),
			            0 0 40px rgba(127, 219, 202, 0.1);
		}
		50% {
			box-shadow: 0 0 30px rgba(127, 219, 202, 0.4),
			            0 0 60px rgba(127, 219, 202, 0.15);
		}
	}

	.signup-card {
		background-color: var(--color-cavern-mid);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		padding: var(--space-8);
		margin-bottom: var(--space-6);
		animation: glow-pulse 4s ease-in-out infinite;
	}

	.signup-form {
		width: 100%;
		max-width: 500px;
		margin: 0 auto;
	}

	.form-group {
		display: flex;
		gap: var(--space-3);
		flex-wrap: wrap;
		justify-content: center;
	}

	.email-input {
		flex: 1;
		min-width: 200px;
		padding: var(--space-3) var(--space-4);
		font-family: var(--font-body);
		font-size: var(--text-base);
		color: var(--color-text-primary);
		background-color: var(--color-bg-page);
		border: 2px solid var(--color-border);
		border-radius: var(--radius-md);
		transition: border-color 0.2s ease;
	}

	.email-input:focus {
		outline: none;
		border-color: var(--color-brand);
	}

	.email-input::placeholder {
		color: var(--color-text-secondary);
	}

	.btn-cta {
		background-color: var(--color-cta);
		color: var(--color-cavern-deep);
		font-family: var(--font-display);
		font-weight: var(--weight-medium);
		padding: var(--space-3) var(--space-6);
		border-radius: var(--radius-md);
		border: none;
		cursor: pointer;
		transition: all 0.2s ease;
	}

	.btn-cta:hover {
		background-color: var(--color-amber-light);
		transform: translateY(-1px);
	}

	.hidden {
		display: none;
	}

	/* Contact Methods */
	.contact-methods {
		margin-top: var(--space-8);
	}

	.contact-method {
		font-size: var(--text-xl);
		margin-bottom: var(--space-4);
	}

	.contact-method:last-child {
		margin-bottom: 0;
	}

	/* Responsive adjustments */
	@media (max-width: 768px) {
		.hero-title {
			font-size: var(--text-4xl);
		}

		.hero {
			padding: var(--space-12) 0;
		}

		.section-about,
		.section-contact {
			padding: var(--space-12) 0;
		}

		.about-features {
			grid-template-columns: 1fr;
		}

		.signup-card {
			padding: var(--space-6);
		}

		.form-group {
			flex-direction: column;
			align-items: stretch;
		}

		.email-input {
			min-width: 100%;
		}

		.btn-cta {
			width: 100%;
		}

		.contact-method {
			font-size: var(--text-lg);
		}
	}
</style>
