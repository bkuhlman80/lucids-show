<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630" fill="none">
  <!-- Background -->
  <rect width="1200" height="630" fill="#0D1518"/>

  <!-- Definitions -->
  <defs>
    <filter id="textGlow">
      <feGaussianBlur stdDeviation="6" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>

    <!-- Subtle background glow behind text -->
    <radialGradient id="centralGlow" cx="0.5" cy="0.5" r="0.5">
      <stop offset="0%" style="stop-color:#7FDBCA;stop-opacity:0.15" />
      <stop offset="100%" style="stop-color:#7FDBCA;stop-opacity:0" />
    </radialGradient>
  </defs>

  <!-- Central subtle glow behind text -->
  <ellipse cx="600" cy="315" rx="500" ry="300" fill="url(#centralGlow)" />

  <!-- Main content - vertically centered with safe margins -->
  <g>
    <!-- LUCIDS wordmark - reduced size for safe margins -->
    <text
      x="600"
      y="280"
      font-family="system-ui, -apple-system, sans-serif"
      font-size="110"
      font-weight="700"
      fill="#7FDBCA"
      text-anchor="middle"
      filter="url(#textGlow)"
      letter-spacing="6"
    >LUCIDS</text>

    <!-- Tagline -->
    <text
      x="600"
      y="360"
      font-family="system-ui, -apple-system, sans-serif"
      font-size="32"
      font-weight="400"
      fill="#A8E6CF"
      text-anchor="middle"
      letter-spacing="1"
    >Dream-tenders of the Hallow</text>

    <!-- Subtitle -->
    <text
      x="600"
      y="420"
      font-family="system-ui, -apple-system, sans-serif"
      font-size="26"
      font-weight="400"
      fill="#888888"
      text-anchor="middle"
    >A children's audio drama podcast</text>
  </g>
</svg>
